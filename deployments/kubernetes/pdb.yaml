apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: chatbox-pdb
  namespace: default
  labels:
    app: chatbox
    component: websocket
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: chatbox
      component: websocket
